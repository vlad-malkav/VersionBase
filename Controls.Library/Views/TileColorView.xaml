<UserControl x:Class="Controls.Library.Views.TileColorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vml="clr-namespace:Controls.Library.VML"
             vml:ViewModelLocator.AutoHookedUpViewModel = "True" 
             mc:Ignorable="d"  Height="200" Width="200">


    <StackPanel Margin="10">
        <ComboBox Name="cmbTileType"
                  ItemsSource = "{Binding ListTileColorModel}"
                  SelectedItem = "{Binding SelectedTileColor}"
                  SelectionChanged="cmbTileColor_SelectionChanged"
                  Width="Auto">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Name}" Width="80" />
                        <Rectangle Width="50" Height="Auto" Fill="{Binding ColorBrush}"/>
                    </StackPanel>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
    </StackPanel>
</UserControl>