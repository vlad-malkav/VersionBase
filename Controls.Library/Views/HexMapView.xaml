<UserControl x:Class="Controls.Library.Views.HexMapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:Controls.Library.ViewModels"
             mc:Ignorable="d"
             Height="{Binding Height}" Width="{Binding Width}">
    <UserControl.Resources>
        <viewModels:HexMapViewModel x:Key="ViewModel"/>
    </UserControl.Resources>

    <Grid DataContext="{DynamicResource ViewModel}">
    <ItemsControl ItemsSource="{Binding ListPolygon}">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <Canvas HorizontalAlignment="Center" VerticalAlignment="Center" Background="Aqua" Width="{Binding Width}" Height="{Binding Height}"/>
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
    </ItemsControl>
    </Grid>
</UserControl>