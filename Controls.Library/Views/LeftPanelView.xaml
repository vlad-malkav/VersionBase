<UserControl x:Class="Controls.Library.Views.LeftPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:Controls.Library.Views"
             xmlns:viewModels="clr-namespace:Controls.Library.ViewModels"
             mc:Ignorable="d" >
    <UserControl.Resources>
        <viewModels:LeftPanelViewModel x:Key="ViewModel"/>
    </UserControl.Resources>
    <Grid Width="200" Height="{Binding ActualHeight, RelativeSource = {RelativeSource AncestorType = {x:Type Grid}}}" Background="Blue" DataContext="{DynamicResource ViewModel}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <views:TileEditorView Grid.Row="0" x:Name="TileEditorViewControl" DataContext="{Binding TileEditorViewModel}" VerticalAlignment="Top"/>
    </Grid>
</UserControl>
