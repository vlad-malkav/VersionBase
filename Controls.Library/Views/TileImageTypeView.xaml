<UserControl x:Class="Controls.Library.Views.TileImageTypeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vml="clr-namespace:Controls.Library.VML"
             vml:ViewModelLocator.AutoHookedUpViewModel = "True" 
             mc:Ignorable="d"  Height="200" Width="200">


    <StackPanel Margin="10">
        <ComboBox Name="cmbTileType"
                  ItemsSource = "{Binding ListTileImageTypeModel}"
                  SelectedItem = "{Binding SelectedTileType}"
                  SelectionChanged="cmbTileImageType_SelectionChanged"
                  Width="Auto">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <!--<Rectangle Fill="{Binding BitmapUri}" Width="16" Height="16" Margin="0,2,5,2" />-->
                        <TextBlock Text="{Binding Name}" Width="Auto" />
                        <Image Width="Auto" Height="20" Source="{Binding BitmapUri}"/>
                    </StackPanel>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
    </StackPanel>
</UserControl>
